# Database (MySQL)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bebii_seo_dashboard

# Optional shared token between plugin and verification API
PLUGIN_SHARED_TOKEN=

# Required if plugin sends encrypted x-digital header
BEBII_GLOBAL_KEY=
